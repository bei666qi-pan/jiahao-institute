# 网页版好友豪气榜

好友榜通过网页邀请链接建立关系，不读取微信好友列表，也不要求用户先注册。

## 用户流程

1. 用户先完成一次单人鉴定。
2. 在结果页或顶部导航打开「好友榜」。
3. 创建好友榜并获得 `/r/{房间码}` 邀请链接与二维码。
4. 好友打开链接，使用自己的最新单人鉴定成绩加入。
5. 房间按「云端可信成绩优先、同类成绩按嘉豪指数降序」排名。
6. 已加入成员可以使用公开成绩进行快速 PK；不会重新上传双方原始素材。

## 隐私边界

房间只保存并展示：

- 公开昵称
- 嘉豪指数
- 豪气等级与物种
- 六维公开分数
- 成绩来源及是否具有服务端凭证

房间不保存、不展示：

- 原始照片
- 聊天截图或文档
- 用户输入的完整文字
- 完整私密判词

## 可信成绩

`social-server.mjs` 会为 `/api/analyze` 的云端成功结果追加 `resultToken`。好友榜服务验证该 HMAC 凭证后，将成绩标记为「云端可信」。

浏览器本地豪之算法生成的降级成绩仍可参与娱乐榜单，但会标记为「基础成绩」，并排在云端可信成绩之后。

生产环境建议设置：

```bash
SOCIAL_SIGNING_SECRET=请使用独立高强度随机值
```

未设置时，服务会依次尝试复用 `ADMIN_PASSWORD`、模型密钥；不建议长期依赖该回退。

## 数据库

社交数据使用现有 `DATABASE_URL`，启动时自动应用 `server/migrations/002_social_rooms.sql`。

主要数据表：

- `jh_social_profiles`
- `jh_rooms`
- `jh_room_members`
- `jh_room_pk_matches`

房间默认 7 天有效，单个匿名身份最多同时创建 10 个有效房间。

## 本地验证

```bash
npm install
npm run build
npm test
npm run test:integration
npm start
```

未配置 `DATABASE_URL` 时，主鉴定和语录功能仍可使用；好友榜接口会明确返回未启用状态。
