export function SceneProp({ view, facts }) {
  if (view?.kind === 'document') return <section className="scene-prop scene-paper" aria-label="文档"><h2>{view.label || '桌上的文档'}</h2>{view.lines?.length ? view.lines.map((line, index) => <p key={index}>{line}</p>) : <p className="scene-prop-muted">还没有正文</p>}</section>;
  if (view?.kind === 'book') return <section className="scene-prop scene-book-wrap" aria-label="手里的书"><div className={`scene-book ${view.label === '近代史' ? 'history' : ''}`}><small>手里的书</small><h2>{view.label}</h2><span aria-hidden="true">━</span></div>{view.note ? <p>{view.note}</p> : null}</section>;
  if (view?.kind === 'photo') return <section className="scene-prop scene-photo-prop" aria-label="相框示意"><div className={`scene-frame ${view.filter && view.present ? 'filtered' : ''} ${view.present ? '' : 'empty'}`} aria-hidden="true">{view.present ? <><img className="scene-selfie-crop" src="/assets/jiahao/hao-universe-hero.webp" alt="嘉豪人物素材"/><svg className="scene-door" viewBox="0 0 70 100"><path d={view.filter ? 'M10 95 Q38 52 10 5 L62 5 L62 95' : 'M10 95 L10 5 L62 5 L62 95'}/><circle cx="52" cy="53" r="2"/></svg></> : <span>—</span>}</div><div><h2>相框示意</h2><p>{!view.present ? '照片已删除' : view.filter ? '滤镜开启' : '滤镜关闭'}</p>{view.present ? <small className="scene-prop-muted">{view.filter ? '门框弯了' : '门框直了'}</small> : null}</div></section>;
  if (view?.kind === 'song') return <section className="scene-prop scene-song" aria-label="此刻的演唱"><h2>此刻的演唱</h2><p>{view.performance}</p></section>;
  return facts.length ? <section className="scene-facts" aria-label="眼前情况"><h2>眼前情况</h2><ul>{facts.map((fact,index)=><li key={index}>{fact}</li>)}</ul></section> : null;
}
